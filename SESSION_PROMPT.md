# 세션 시작 프롬프트

아래 프롬프트를 새 Claude Code 세션에서 **첫 메시지**로 붙여넣으세요.

---

## 프롬프트 (복사용)

```
sustainreport_ai 구현 세션을 시작합니다.

## 1. 컨텍스트 부트스트랩 (필수 읽기)

아래 파일을 **순서대로** 읽고 현재 상태를 파악하세요:

1. `CLAUDE.md` — 오케스트레이터 헌법 (전체 아키텍처, 금지사항, ID 체계)
2. `IMPLEMENTATION_GUIDE.md` — 구현 순서와 기술 결정
3. `SESSION_LOG.md` — 이전 세션 완료/미완료 기록 ← 여기서 이어받을 작업 확인
4. `agents/section-writer.md` — 에이전트 프롬프트 패턴 (다른 에이전트 작성 시 참조)
5. `schemas/*.schema.json` — 이미 정의된 스키마 확인

그 다음 실제 파일 현황을 확인하세요:
- `ls orchestration/` `ls scripts/` `ls agents/` `ls schemas/` `ls templates/` `ls llm/`

## 2. 세션 작업 규칙

### 작업 단위 = Step
IMPLEMENTATION_GUIDE.md의 Step 단위로 작업합니다. 한 세션에서 1~2 Step만 집중합니다.

### 서브에이전트 활용 원칙
- **병렬 가능한 파일**은 Agent 도구로 동시 디스패치 (예: 에이전트 프롬프트 6개를 3쌍으로 병렬)
- 각 서브에이전트에게는 **해당 파일만 생성**하도록 명확한 입출력 지시
- 서브에이전트 prompt에 반드시 포함할 것:
  - `agents/section-writer.md`의 구조 패턴
  - 해당 에이전트의 역할·입출력 계약 (CLAUDE.md의 에이전트 일람 참조)
  - `schemas/` 내 관련 스키마 내용 (있으면)
  - **생성할 파일의 절대 경로**

### 컨텍스트 보호 규칙
- 긴 파일 전체를 읽지 말 것 → 필요한 부분만 offset/limit으로 읽기
- 이미 완성된 파일은 다시 읽지 말 것 → SESSION_LOG.md에서 완료 여부 확인
- 탐색(Explore)과 구현을 분리 → 탐색은 Explore 에이전트, 구현은 직접 또는 general-purpose 에이전트

### 세션 종료 시 반드시 할 것
SESSION_LOG.md에 아래 형식으로 추가:

```markdown
## 세션 YYYY-MM-DD #N

### 완료
- [x] Step X: {파일명} 생성 완료
- [x] Step X: {파일명} 생성 완료

### 미완료 / 다음 세션에서 이어갈 것
- [ ] Step X: {구체적으로 무엇을 해야 하는지}
- [ ] 발견된 이슈: {내용}

### 결정 사항
- {이번 세션에서 내린 설계 결정이 있으면 기록}
```

## 3. 작업 시작

SESSION_LOG.md를 읽고:
- 파일이 없으면: "첫 세션입니다. Step 1부터 시작합니다" 라고 알려주세요.
- 파일이 있으면: 마지막 세션의 미완료 항목을 보여주고 이어받을지 물어보세요.

**자동으로 코드를 작성하지 마세요.** 현재 상태를 보고한 뒤, 이번 세션에서 할 작업 목록을 제안하고 내 확인을 받은 후 진행하세요.
```

---

## 사용 방법

1. 새 세션을 열고 위 프롬프트를 붙여넣기
2. Claude가 상태를 보고하면 → 작업 범위 확인/조정
3. 작업 진행
4. 세션 끝나기 전에 "세션 종료" 라고 말하면 → SESSION_LOG.md 자동 갱신
5. 다음 세션에서 같은 프롬프트로 시작 → 자동으로 이어받기
